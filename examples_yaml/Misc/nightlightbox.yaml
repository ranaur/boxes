# Generated YAML configuration for NightLightBox
# Full format with Defaults and Boxes sections
# This file can be used with boxes_generator.py or the build command

Defaults:
    # reference: 0

Boxes:
  - box_type: NightLightBox
    name: "NightLightBox_example"
    generate: true
    args:
      ### Settings for Finger Joints
      ## height of the bottom lips sticking out  (multiples of thickness) FingerHoleEdge only!
      # FingerJoint_bottom_lip: 0.0
      ## space below holes of FingerHoleEdge (multiples of thickness)
      # FingerJoint_edge_width: 1.0
      ## extra material to grind away burn marks (multiples of thickness)
      # FingerJoint_extra_length: 0.0
      ## width of the fingers (multiples of thickness)
      # FingerJoint_finger: 2.0
      ## extra space to allow finger move in and out (multiples of thickness)
      # FingerJoint_play: 0.0
      ## space between fingers (multiples of thickness)
      # FingerJoint_space: 2.0
      ## style of the fingers
      ## Valid values: rectangular, springs, barbs, snap
      # FingerJoint_style: "rectangular"
      ## space at the start and end in multiple of normal spaces
      # FingerJoint_surroundingspaces: 1.0
      ## width of finger holes (multiples of thickness)
      # FingerJoint_width: 1.0

      ### Settings for Stackable Edges
      ## inside angle of the feet
      # Stackable_angle: 60
      ## height of strips to be glued to the inside of bottom edges (multiples of thickness)
      # Stackable_bottom_stabilizers: 0.0
      ## height of the feet (multiples of thickness)
      # Stackable_height: 2.0
      ## distance from finger holes to bottom edge (multiples of thickness)
      # Stackable_holedistance: 1.0
      ## width of the feet (multiples of thickness)
      # Stackable_width: 4.0

      ### Settings for Hinges and HingePins
      ## diameter of the pin hole (multiples of thickness)
      # Hinge_axle: 2.5
      ## fixed length of the grips on he lids (multiples of thickness)
      # Hinge_grip_length: 0
      # Hinge_grip_percentage: 0
      ## thickness of the arc holding the pin in place (multiples of thickness)
      # Hinge_hingestrength: 1
      ## have lid overlap at the sides (similar to OutSetEdge)
      # Hinge_outset: true
      ## set to lower value to get disks surrounding the pins
      # Hinge_pinwidth: 0.4

      ### Night lightbox diffuser plate lock to prevent unwanted access to the electronics
      ## Thickness of the background acrylic diffuser plate locking nut in mm
      # DiffuserPlateTLockNutThickness: 2.1
      ## Width of the background acrylic diffuser plate locking nut in mm
      # DiffuserPlateTLockNutWidth: 5.1
      ## Diameter of the background acrylic diffuser plate locking screw hole in mm
      # DiffuserPlateTLockScrewDiameter: 3.0
      ## Length of the background acrylic diffuser plate locking screw in mm
      # DiffuserPlateTLockScrewLength: 20.0

      ### Night lightbox options for the back side (holes for connectors, marking)
      ## extra holes for connectors or buttons ; enter one line per hole ; first parameter should be R for rectangle or C for circle ; then X and Y position for the center of the hole, and then the X and Y size of the rectangle or the circle diameter, all in mm ; parameters should be separated by spaces
      # BackExtraHoles: "R 20 15 11.5 8
C 11.58 15 3
C 28.42 15 3"

      ### NightLightBox Settings
      ## Depth of the background zone for the electronics and LEDs in mm
      # BackgroundDepth: 40.0
      ## style of the front lock
      # BoxStyle: "large face"
      ## Thickness of the background acrylic diffuser plate in mm
      # DiffuserPlateThickness: 5.0
      ## Space between the decorative plates in mm
      # InterPlateSpacing: 10
      ## Margin for moving parts in mm
      # Margin: 0.5
      ## height of the window in the front panel in mm
      # PlateVisibleHeight: 75.0
      ## width of the window in the front panel in mm
      # PlateVisibleWidth: 150.0
      ## Thickness of the wood plates in mm
      # WoodPlateThickness: 5.0
      ## Number of decorative wood plates
      # WoodPlatesCount: 3
      ## add hooks to decorative plates (allowing one sides plates)
      # hooks: false

      ### Default Settings
      ## burn correction (in mm)(bigger values for tighter fit) [🛈](https://florianfesti.github.io/boxes/html/usermanual.html#burn)
      # burn: 0.1
      ## print surrounding boxes for some structures [🛈](https://florianfesti.github.io/boxes/html/usermanual.html#debug)
      # debug: false
      ## format of resulting file [🛈](https://florianfesti.github.io/boxes/html/usermanual.html#format)
      # format: "svg"
      ## style for inner corners [🛈](https://florianfesti.github.io/boxes/html/usermanual.html#inner-corners)
      # inner_corners: "loop"
      ## label the parts (where available)
      # labels: true
      ## Add a QR Code with link or command line to the generated output
      # qr_code: false
      ## print reference rectangle with given length (in mm)(zero to disable) [🛈](https://florianfesti.github.io/boxes/html/usermanual.html#reference)
      # reference: 100.0
      ## spacing around parts (multiples of thickness [: extra space in mm]) [🛈](https://florianfesti.github.io/boxes/html/usermanual.html#spacing)
      # spacing: "0.5"
      ## width of tabs holding the parts in place (in mm)(not supported everywhere) [🛈](https://florianfesti.github.io/boxes/html/usermanual.html#tabs)
      # tabs: 0.0
      ## thickness of the material (in mm) [🛈](https://florianfesti.github.io/boxes/html/usermanual.html#thickness)
      # thickness: 3.0
